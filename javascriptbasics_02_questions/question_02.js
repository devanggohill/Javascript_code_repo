// Write a JavaScript program that:

// Takes a number N from the user using prompt().
// Prints all numbers from 1 to N.

let num = Number(prompt("Enter a number:"));

for (let i = 1; i <= num; i++) {
    console.log(i);
}